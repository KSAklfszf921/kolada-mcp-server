# Kolada MCP Server Configuration

# Server Mode (stdio or http)
MCP_MODE=http

# HTTP Server Port (only used in HTTP mode)
PORT=3000

# Authentication Token (required for production in HTTP mode)
# Generate a secure token: openssl rand -base64 32
MCP_AUTH_TOKEN=your-secret-token-here

# Kolada API Configuration (optional)
# KOLADA_API_BASE_URL=https://api.kolada.se/v3
# KOLADA_RATE_LIMIT=5
# KOLADA_CACHE_TTL=86400
# KOLADA_TIMEOUT=30000
